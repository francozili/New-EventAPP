{
  "name": "EventApp_project",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ..",
    "postinstall": "npm run build && echo 'Client built!'"
  },
  "dependencies": {
    "dm": "^0.5.0",
    "dom": "0.0.3",
    "react": "^16.8.6",
    "react-router-dom": "^5.0.1",
    "router": "^1.3.3"
  }
}
